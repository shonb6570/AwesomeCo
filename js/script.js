// Fetch functions

function createUser(element) {
    return document.createElement(element);
}


function appendElement(parent, element) {
    return document.appendChild(element);
}


// fetch request
fetch('https://randomuser.me/api/?results=12')
    .then(function (data) {

    })
    .catch(function (error) {

    });

// get employee name from ajax request

// store employee-card div in a variable

//create <p> element and append employee name


//repeat for image, first and last name, email and city


//create a modal window

//add the following extra info in modal only - cell, detailed address and birth date